# CCFOLIA Manager 使い方

**Version 2.0.0**  
**Author: L4zz**

CCFOLIA Manager は、キャラクター・画像・BGMをローカルで管理するWindowsアプリと、CCFOLIA上から素早く操作するためのブラウザ拡張機能を組み合わせて使用します。

> 本ソフトウェアは非公式ツールです。ココフォリア株式会社、ココフォリア公式およびその運営とは関係ありません。

## 1. 本体とブラウザパネルの役割

Windowsアプリ本体は、Character / Images / BGMの各データをまとめて管理するために使用します。

CCFOLIA上のブラウザパネルは、セッション中にCCFOLIAから離れず、検索・送信・背景変更・BGM切り替え等を行うために使用します。

ローカルデータをCCFOLIA Manager側で管理し、必要なデータをCCFOLIAへ送る運用を基本とします。

## 2. 初回セットアップ

### CCFOLIA Manager本体を起動

配布ZIPを展開し、`CCFOLIAManager.exe` を起動します。

CCFOLIA Managerは展開したフォルダ内へデータを書き込むため、`Program Files` 直下よりも、Documentsや任意のAppsフォルダなど、通常ユーザーが書き込み可能な場所への展開を推奨します。

### ブラウザ拡張機能を読み込む

- Brave: `brave://extensions/`
- Google Chrome: `chrome://extensions/`
- Microsoft Edge: `edge://extensions/`

デベロッパーモードを有効にし、「パッケージ化されていない拡張機能を読み込む」または「展開済みを読み込む」を選択して、配布物内の `browser_extension` フォルダを指定します。

### CCFOLIAを開く

CCFOLIAのルームを開き、必要に応じてページを再読み込みします。

CCFOLIA Manager本体が起動していれば、ブラウザパネルからローカルManagerへ接続できます。

---

# Character

Characterの操作は、CCFOLIA Character Manager 1.xの機能を引き継いでいます。

## 3. キャラクターを作成する

Windowsアプリ本体で `＋キャラクター` を押します。

キャラクター編集画面では、主に以下のタブを使用します。

- 基本情報
- ステータス
- パラメータ
- 技能
- メモ
- チャットパレット
- 分類

新規キャラクターは編集モードで開きます。既存キャラクターは閲覧モードで開き、必要に応じて編集へ切り替えます。

## 4. キャラクター画像を登録する

キャラクター編集画面では複数画像を登録できます。

- 画像追加
- ドラッグ＆ドロップ
- 並び替え
- 主画像の変更
- 画像削除

主画像はキャラクター一覧やブラウザパネル上のアイコンとして使用されます。ブラウザ側の編集画面からも画像を追加できます。

## 5. タグを使う

キャラクターには複数のタグを設定できます。

例: `NPC`、`敵`、`商人`、`地域A`

ブラウザパネルのタグ検索では、選択したタグが検索バー内にカードとして表示されます。複数タグを指定した場合は **AND検索** になります。

例えば `[ 地域A × ] [ NPC × ]` の場合、「地域A」と「NPC」の両方のタグを持つキャラクターだけが表示されます。

タグは入力候補または一覧から選択できます。

## 6. グループを使う

キャラクターはグループへ分類できます。グループは階層化できます。

例:

```text
地域A
├─ 街
├─ 商会
└─ 施設
```

Windowsアプリ本体ではグループをドラッグして並び替え・階層変更できます。ブラウザパネルでもグループでキャラクターを絞り込めます。

`未分類` を選ぶと、どのグループにも所属していないキャラクターだけを表示できます。

## 7. Quick Memoを使う

CCFOLIA上のブラウザパネルでキャラクターを左クリックするとQuick Memoを開けます。

セッション中の一時的な記録などに使用できます。

例:

- このNPCはPCと面識あり
- 負傷中
- 次回交渉予定
- アイテムを預けた

Quick Memoは自動保存されます。

パネル外をクリックするとブラウザパネルだけを一時的に隠せます。再度CCFOLIA Managerボタンを押すと、直前に開いていた状態を保持したまま戻ります。

## 8. ブラウザパネルから編集する

キャラクターを右クリックするとコンテキストメニューを開けます。

主な操作:

- 編集
- Memo
- グループ追加
- タグ追加
- 複製
- ゴミ箱

編集画面は背景や空白部分をクリックしても閉じません。右上の `×` から閉じます。

## 9. CCFOLIAへ送信する

1キャラクターだけ送る場合は、Windowsアプリまたはブラウザパネルから対象キャラクターの送信操作を実行します。

複数キャラクターを送る場合は、複数キャラクターを選択して `選択を送る` を実行します。

同じキャラクターを再送する場合、CCFOLIA Managerは同じ送信用IDを使用するため、CCFOLIA上の同一キャラクターを更新する用途に使用できます。複製したキャラクターには別の送信用IDが割り当てられます。

## 10. 複数キャラクターをまとめて操作する

複数選択時には、まとめて以下の操作ができます。

- CCFOLIAへ送信
- タグ追加
- グループ追加
- ゴミ箱へ移動

一括タグ追加・一括グループ追加は、既存の分類を残したまま追加します。

## 11. メモについて

通常のメモとは別に、複数の名前付きメモを作成できます。

メモには `ココフォリアへ出力` の設定があります。

- ON: CCFOLIAへ送るデータへ含めます。
- OFF: CCFOLIA Manager内だけで管理します。

セッション運営用の非公開メモなどはOFFで管理できます。

## 12. チャットパレット

チャットパレットを登録できます。

CCFOLIAから取り込んだキャラクターについては、既存のコマンドを可能な限りそのまま保持します。CCFOLIAへ送る際にチャットパレットとして出力されます。

## 13. キャラクターを削除する

通常の削除では、キャラクターはすぐには消えず `ゴミ箱` へ移動します。

ゴミ箱では以下を使用できます。

- 復元
- 完全に削除
- すべて完全削除

`完全に削除` および `すべて完全削除` は元に戻せません。完全削除すると、キャラクターデータと関連画像も削除されます。

## 14. CCFOLIA上のキャラクターを取り込む

ブラウザ拡張機能を利用して、CCFOLIAルーム上の既存キャラクターをCCFOLIA Managerへ取り込めます。

すでに同じ取り込み元IDを持つキャラクターが存在する場合は、重複作成を避けるため既存データを基準に処理します。

大量に取り込む前にはバックアップを推奨します。

---

# Images

## 15. Imagesについて

Imagesでは、セッションで使用する背景画像等をローカルライブラリとして管理します。

デスクトップアプリとブラウザパネルの両方から画像を参照できます。

## 16. 画像を追加する

画像ファイルは、CCFOLIA ManagerのImages画面またはブラウザパネルから追加できます。

対応している画面では、ファイルのドラッグ＆ドロップでも追加できます。

追加された画像はローカルライブラリへ登録されます。

## 17. 画像を検索・閲覧する

画像ライブラリから目的の画像を探して使用できます。

大量の画像を登録している場合はページ単位で表示されるため、全画像を一度に読み込まずに利用できます。

## 18. CCFOLIAの背景へ適用する

ブラウザパネルのImagesから画像を選び、現在開いているCCFOLIAルームの背景へ適用できます。

CCFOLIA側へすでにアップロード済みの画像についても、保存されている情報を利用して背景として適用できます。

## 19. CCFOLIA上の画像を取り込む

CCFOLIA上ですでに利用している画像データを、CCFOLIA Managerへ取り込めます。

取り込み後はローカルのImagesライブラリから再利用できます。

---

# BGM

## 20. BGMについて

BGMでは、セッションで使用する音声をローカルライブラリとして管理します。

デスクトップアプリとブラウザパネルの両方から検索・選択できます。

## 21. BGMを追加する

BGMファイルをローカルライブラリへ追加できます。

対応している画面ではドラッグ＆ドロップでも追加できます。

登録したBGMには、必要に応じて以下を設定できます。

- 名前
- 音量
- ループ
- 種別
- タグ
- グループ

## 22. CCFOLIAへBGMを登録する

ブラウザパネルから、ローカルのBGMをCCFOLIA側へアップロード・登録できます。

CCFOLIAとの通信に必要な認証情報はブラウザ側で一時的に利用し、CCFOLIA Managerのローカルデータベースへ永続保存しない設計です。

## 23. ルームBGMを切り替える

ブラウザパネルからBGMを選択すると、現在開いているCCFOLIAルームのBGMを切り替えられます。

事前にCCFOLIA側へ登録済みのBGMは、ライブラリから再選択して利用できます。

## 24. CCFOLIA上のBGMを取り込む

CCFOLIAアカウント上に存在する既存BGMの情報をCCFOLIA Managerへ取り込めます。

取り込み後はローカルBGMライブラリから検索・分類・再利用できます。

---

# 共通操作

## 25. バックアップ

大量のデータを編集・削除・取り込む前や、CCFOLIA Managerを更新する前にはバックアップを推奨します。

特に以下の前にバックアップすると安全です。

- 大量のキャラクターを編集する前
- 大量削除する前
- CCFOLIAから大量にデータを取り込む前
- CCFOLIA Managerを更新する前
- PCを移行する前

## 26. データの保存場所

ユーザーデータは、CCFOLIA Managerを展開したフォルダ内の `app_data` を中心に保存されます。

CCFOLIA Managerを別PCへ移動する場合は、アプリフォルダ全体をコピーするのが簡単です。

## 27. CCFOLIA Managerを更新する

新しいバージョンが公開された場合は、GitHubのReleasesから最新版を取得します。

更新時は、既存のユーザーデータを削除しないでください。

プログラム部分を最新版へ差し替えたあと、ブラウザ拡張機能も拡張機能管理画面から再読み込みしてください。

## 28. ブラウザパネルが接続できない場合

以下を確認してください。

1. `CCFOLIAManager.exe` が起動している
2. ブラウザ拡張機能が有効
3. CCFOLIAのページを再読み込みした
4. 拡張機能が現在使用している `browser_extension` フォルダを参照している
5. CCFOLIA Manager本体を別フォルダへ移動した場合、必要に応じて拡張機能を読み込み直した

CCFOLIA Manager本体とブラウザ拡張機能は `127.0.0.1:17431` でローカル通信します。

## 29. CCFOLIA連携が動作しない場合

まず、CCFOLIA Manager本体とブラウザ拡張機能が接続されていることを確認してください。

そのうえで、以下を試してください。

- CCFOLIAページを再読み込み
- ブラウザ拡張機能を再読み込み
- CCFOLIA Manager本体を再起動

CCFOLIA側の仕様変更によって、一部機能が一時的に動作しなくなる場合があります。

## 30. ブラウザ拡張機能を更新する

CCFOLIA Manager本体を更新したあと、ブラウザ側も更新してください。

- Brave: `brave://extensions/`
- Chrome: `chrome://extensions/`
- Edge: `edge://extensions/`

CCFOLIA Manager拡張機能の `再読み込み` を押し、その後CCFOLIAのページを再読み込みします。

## 31. 不具合を報告する

GitHubのIssuesから報告してください。

可能であれば、以下を記載してください。

- CCFOLIA Managerのバージョン
- Windowsのバージョン
- 使用ブラウザ
- 何をしようとしたか
- 表示されたエラー
- 再現手順

キャラクターデータ、個人情報、非公開セッションデータ、認証情報等はそのまま添付しないでください。

## 32. 注意事項

- 本ソフトウェアは非公式ツールです。
- ココフォリア株式会社、ココフォリア公式およびその運営とは関係ありません。
- CCFOLIA側の仕様変更によって動作しなくなる場合があります。
- Windows Authenticode署名は行っていないため、SmartScreenで「不明な発行元」と表示される場合があります。
- 利用条件については `TERMS_OF_USE.md` を確認してください。

## 作者

**L4zz**
