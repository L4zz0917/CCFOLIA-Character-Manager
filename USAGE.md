# CCFOLIA Character Manager 使い方

**Version 1.0.0**  
**Author: L4zz**

CCFOLIA Character Manager（CCM）は、キャラクターの作成・管理を行うWindowsアプリと、CCFOLIA上で素早く検索・編集・送信するためのブラウザ拡張機能を組み合わせて使用します。

> 本ソフトウェアは非公式ツールです。ココフォリア公式およびその運営とは関係ありません。

## 1. 本体とブラウザパネルの役割

Windowsアプリ本体は、キャラクターの作成・詳細編集・タグやグループの整理・画像管理・バックアップなど、日常的な管理に使用します。

CCFOLIA上のCCMサイドパネルは、セッション中の検索、Quick Memo、簡易編集、複数選択、CCFOLIAへの送信など、素早い操作に使用します。

## 2. 初回セットアップ

### CCM本体を起動

配布ZIPを展開し、`CCFOLIACharacterManager.exe` を起動します。

CCMは展開したフォルダ内へデータを書き込むため、`Program Files` 直下よりも、Documentsや任意のAppsフォルダなど、通常ユーザーが書き込み可能な場所への展開を推奨します。

### ブラウザ拡張機能を読み込む

ブラウザごとに以下を開きます。

- Brave: `brave://extensions/`
- Google Chrome: `chrome://extensions/`
- Microsoft Edge: `edge://extensions/`

デベロッパーモードを有効にし、「パッケージ化されていない拡張機能を読み込む」を選択して、配布物内の `browser_extension` フォルダを指定します。

ブラウザやバージョンによっては、「展開済みを読み込む」等の表記になる場合があります。

### CCFOLIAを開く

CCFOLIAのルームを開き、必要に応じてページを再読み込みします。

CCM本体が起動していれば、CCMサイドパネル上の接続状態が `ローカル接続` になります。

## 3. キャラクターを作成する

Windowsアプリ本体で `＋キャラクター` を押します。

キャラクター編集画面では、主に以下のタブを使用します。

- 基本情報
- ステータス
- パラメータ
- 技能
- メモ
- チャットパレット
- 分類

新規キャラクターは編集モードで開きます。既存キャラクターは閲覧モードで開き、必要に応じて編集へ切り替えます。

## 4. 画像を登録する

キャラクター編集画面では複数画像を登録できます。

- 画像追加
- ドラッグ＆ドロップ
- 並び替え
- 主画像の変更
- 画像削除

主画像はキャラクター一覧やブラウザパネル上のアイコンとして使用されます。ブラウザ側の編集画面からも画像を追加できます。

## 5. タグを使う

キャラクターには複数のタグを設定できます。

例: `王国`、`NPC`、`ボス`、`商人`

ブラウザパネルのタグ検索では、選択したタグが検索バー内にカードとして表示されます。複数タグを指定した場合は **AND検索** になります。

例えば `[ 王国 × ] [ NPC × ]` の場合、「女王の森」と「NPC」の両方のタグを持つキャラクターだけが表示されます。

タグは入力候補または右端の一覧から選択できます。

## 6. グループを使う

キャラクターはグループへ分類できます。グループは階層化できます。

例:

```text
王国
├─ 城
├─ 商会
└─ 館
```

Windowsアプリ本体ではグループをドラッグして並び替え・階層変更できます。ブラウザパネルでもグループでキャラクターを絞り込めます。

`未分類` を選ぶと、どのグループにも所属していないキャラクターだけを表示できます。

## 7. Quick Memoを使う

CCFOLIA上のCCMサイドパネルでキャラクターを左クリックするとQuick Memoを開けます。

セッション中の一時的な記録などに使用できます。

例:

- このNPCはPCと面識あり
- 右腕負傷
- 次回交渉予定
- アイテムを預けた

Quick Memoは自動保存されます。

パネル外をクリックするとCCMサイドパネルだけを一時的に隠せます。再度CCMボタンを押すと、直前に開いていた状態を保持したまま戻ります。

## 8. ブラウザパネルから編集する

キャラクターを右クリックするとコンテキストメニューを開けます。

主な操作:

- 編集
- Memo
- グループ追加
- タグ追加
- 複製
- ゴミ箱

編集画面は背景や空白部分をクリックしても閉じません。右上の `×` から閉じます。

## 9. CCFOLIAへ送信する

1キャラクターだけ送る場合は、Windowsアプリまたはブラウザパネルから対象キャラクターの送信操作を実行します。

複数キャラクターを送る場合は、複数キャラクターを選択して `選択を送る` を実行します。

同じキャラクターを再送する場合、CCMは同じ送信用IDを使用するため、CCFOLIA上の同一キャラクターを更新する用途に使用できます。複製したキャラクターには別の送信用IDが割り当てられます。

## 10. 複数キャラクターをまとめて操作する

複数選択時には、まとめて以下の操作ができます。

- CCFOLIAへ送信
- タグ追加
- グループ追加
- ゴミ箱へ移動

一括タグ追加・一括グループ追加は、既存の分類を残したまま追加します。

## 11. メモについて

通常のメモとは別に、複数の名前付きメモを作成できます。

メモには `ココフォリアへ出力` の設定があります。

- ON: CCFOLIAへ送るデータへ含めます。
- OFF: CCM内だけで管理します。

セッション運営用の非公開メモなどはOFFで管理できます。

## 12. チャットパレット

チャットパレットを登録できます。

CCFOLIAから取り込んだキャラクターについては、既存のコマンドを可能な限りそのまま保持します。CCFOLIAへ送る際にチャットパレットとして出力されます。

## 13. キャラクターを削除する

通常の削除では、キャラクターはすぐには消えず `ゴミ箱` へ移動します。

ゴミ箱では以下を使用できます。

- 復元
- 完全に削除
- すべて完全削除

`完全に削除` および `すべて完全削除` は元に戻せません。完全削除すると、キャラクターデータと関連画像も削除されます。

## 14. バックアップ

Windowsアプリ本体からバックアップを作成できます。

以下のタイミングでバックアップを推奨します。

- 大量のキャラクターを編集する前
- 大量削除する前
- CCMを更新する前
- PCを移行する前

## 15. データの保存場所

CCMのユーザーデータは、CCMを展開したフォルダ内に保存されます。

主なフォルダ:

```text
app_data
backups
exports
```

CCMを別PCへ移動する場合は、アプリフォルダ全体をコピーするのが最も簡単です。

## 16. CCMを更新する

新しいバージョンが公開された場合は、GitHubのReleasesから最新版を取得します。

更新時は、既存の `app_data`、`backups`、`exports` を消さないでください。これらにキャラクターや設定等のユーザーデータが保存されています。

プログラム部分を最新版へ差し替えたあと、ブラウザ拡張機能も拡張機能管理画面から再読み込みしてください。

## 17. CCMサイドパネルが「ローカル接続」にならない場合

以下を確認してください。

1. `CCFOLIACharacterManager.exe` が起動している
2. ブラウザ拡張機能が有効
3. CCFOLIAのページを再読み込みした
4. 拡張機能が現在使用している `browser_extension` フォルダを参照している
5. CCM本体を別フォルダへ移動した場合、必要に応じて拡張機能を読み込み直した

CCM本体とブラウザ拡張機能は `127.0.0.1:17431` でローカル通信します。

## 18. CCFOLIAへ送信できない場合

まず、CCMサイドパネルが `ローカル接続` になっていることを確認してください。

そのうえで、以下を試してください。

- CCFOLIAページを再読み込み
- ブラウザ拡張機能を再読み込み
- CCM本体を再起動

CCMを別フォルダへ移動したあとに問題が起きた場合は、ブラウザの拡張機能管理画面から古いCCM拡張機能を削除し、新しい場所の `browser_extension` を読み込み直してください。

## 19. ブラウザ拡張機能を更新する

CCM本体を更新したあと、ブラウザ側も更新してください。

- Brave: `brave://extensions/`
- Chrome: `chrome://extensions/`
- Edge: `edge://extensions/`

CCM拡張機能の `再読み込み` を押し、その後CCFOLIAのページを再読み込みします。

## 20. 不具合を報告する

GitHubのIssuesから報告してください。

可能であれば、以下を記載してください。

- CCMのバージョン
- Windowsのバージョン
- 使用ブラウザ
- 何をしようとしたか
- 表示されたエラー
- 再現手順

キャラクターデータ、個人情報、非公開セッションデータ、認証情報等はそのまま添付しないでください。

## 21. 注意事項

- 本ソフトウェアは非公式ツールです。
- ココフォリア公式およびその運営とは関係ありません。
- CCFOLIA側の仕様変更によって動作しなくなる場合があります。
- Windows Authenticode署名は行っていないため、SmartScreenで「不明な発行元」と表示される場合があります。
- 利用条件については `TERMS_OF_USE.md` を確認してください。

## 作者

**L4zz**
