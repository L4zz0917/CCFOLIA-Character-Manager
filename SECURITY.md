# Security Policy

## 対象

CCFOLIA Manager 2.x の最新版を対象とします。

過去バージョンについては、原則として最新版への更新を推奨します。

## セキュリティ上の問題を見つけた場合

一般の不具合とは異なり、以下のような悪用可能性のある問題については、公開Issueへ秘密情報や詳細な攻撃手順を書き込まないでください。

- 第三者による任意コード実行
- ローカルデータへの不正アクセス
- bridgeのアクセス制御回避
- 認証情報の漏えい
- 意図しない外部サイトからのローカルbridge操作
- その他、利用者のデータやアカウントへ影響する問題

GitHubのPrivate vulnerability reportingが利用可能な場合は、その機能を利用してください。

公開Issueで連絡する必要がある場合は、最初の投稿では秘密情報・認証情報・攻撃に必要な具体情報を含めず、問題の存在と影響範囲だけを記載してください。

## ローカルbridgeについて

CCFOLIA Manager本体とブラウザ拡張機能は `127.0.0.1:17431` を利用してローカル通信します。

bridgeでは、通常のWebページ等からの意図しないアクセスを抑えるため、Host / Origin / 専用クライアントヘッダー等の検証を行っています。

bridgeをインターネット上の外部インターフェースとして公開することは想定していません。

## CCFOLIAとの認証情報について

一部のCCFOLIA連携機能では、ブラウザ上のCCFOLIAセッションで利用される認証情報を一時的に使用します。

これらの認証情報は、CCFOLIA ManagerのSQLiteデータベースや通常のローカル保存領域へ永続保存しない設計です。

Issue、ログ、スクリーンショット等を共有する場合は、トークン、Cookie、Authorizationヘッダー等が含まれていないことを確認してください。

## ユーザーデータ

Issueや公開された場所へ、以下をそのまま投稿しないでください。

- `app_data` の丸ごと添付
- データベースファイル
- 個人情報
- 非公開TRPGセッションのデータ
- キャラクターや画像等に含まれる非公開情報
- 外部サービスのトークン、Cookie、認証情報

不具合報告にデータが必要な場合は、再現に必要な最小限の内容へ加工してください。

## CCFOLIA内部仕様への依存

CCFOLIA Managerの一部機能は、CCFOLIAの公開されていない内部仕様に依存しています。

CCFOLIA側の変更によって動作不能や想定外の挙動が発生する可能性があります。

最新版を利用し、重要なデータについては事前にバックアップしてください。

## 第三者コンポーネント

CCFOLIA ManagerはPySide6 / Qt / PyInstaller等の第三者コンポーネントを利用します。

各コンポーネント固有の脆弱性や更新情報については、それぞれのプロジェクトの情報も確認してください。
