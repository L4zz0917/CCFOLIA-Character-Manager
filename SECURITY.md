# Security Policy

## 対象

CCFOLIA Character Manager 1.x の最新版を対象とします。

## セキュリティ上の問題を見つけた場合

一般の不具合とは異なり、第三者による任意コード実行、ローカルデータへの不正アクセス、bridgeのアクセス制御回避など、悪用可能性のある問題は公開Issueへ詳細を書き込まないでください。

現時点では専用の非公開報告窓口を設けていないため、公開後にGitHubのPrivate vulnerability reportingを有効化できる場合は、その機能を利用する予定です。

## bridgeについて

CCM本体とブラウザ拡張機能は `127.0.0.1:17431` を利用してローカル通信します。

bridgeではHost / Origin / 専用クライアントヘッダーの検証を行っています。

## ユーザーデータ

Issueへ以下を投稿しないでください。

- `app_data` の丸ごと添付
- 個人情報
- 非公開TRPGセッションのデータ
- 外部サービスのトークン、Cookie、認証情報
